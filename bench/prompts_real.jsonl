{"prompt": "Write a complete Python module implementing an LRU cache with TTL expiry, thread safety, and type hints. Include a docstring for every method, a small usage example, and unit tests using pytest. Explain your design decisions at the end."}
{"prompt": "Explain how a modern SSD's flash translation layer works: mapping tables, garbage collection, wear leveling, write amplification, and TRIM. Use headers and be thorough but precise."}
{"prompt": "Skriv en grundig artikel på dansk om, hvordan man planlægger og gennemfører en to-ugers cykeltur langs den danske vestkyst. Inkluder pakkeliste, ruteforslag pr. dag, budget og sikkerhedsråd."}
{"prompt": "Refactor this code for readability and performance, then explain each change:\n\n```python\ndef f(d):\n    r = []\n    for k in d.keys():\n        if d[k] != None:\n            if type(d[k]) == list:\n                for i in range(len(d[k])):\n                    if d[k][i] > 0:\n                        r.append((k, d[k][i]))\n            else:\n                if d[k] > 0:\n                    r.append((k, d[k]))\n    s = sorted(r, key=lambda x: x[1], reverse=True)\n    out = {}\n    for k, v in s:\n        if k in out:\n            out[k] = out[k] + [v]\n        else:\n            out[k] = [v]\n    return out\n```"}
{"prompt": "A train leaves city A at 9:00 traveling at 80 km/h toward city B, 420 km away. A second train leaves B at 10:30 toward A at 100 km/h. When and where do they meet? Then generalize: give the formula for arbitrary speeds, distances and departure offsets, and check it against the numbers."}
{"prompt": "Produce a JSON document describing a fictional e-commerce order system schema: 6 tables with columns, types, constraints, indexes and relations, followed by SQL DDL for PostgreSQL and three example analytical queries with explanations."}
{"prompt": "Write a detailed technical blog post comparing Rust's ownership model with C++ RAII and Go's garbage collector, with code samples in all three languages and a section on when each approach wins."}
{"prompt": "Summarize the following text and then list the ten most important facts as bullet points:\n\nThe Danish energy island project in the North Sea was conceived as a hub that collects power from surrounding offshore wind farms and distributes it to several countries. Initially planned with a capacity of 3 GW and later 10 GW, the artificial island would sit roughly 80 km off the coast of Jutland. In 2023 the tender was postponed after cost estimates rose sharply, and the government asked for a redesign that could reduce the state's financial exposure. Alternatives discussed included platform-based solutions instead of a reclaimed island, phased construction, and closer integration with Belgian and German grids. Critics argued that delays put Denmark's 2030 climate targets at risk, while proponents said a cheaper design would still deliver the same electricity. Meanwhile, the Baltic Sea energy island around Bornholm proceeded on a different model, using the existing island as the hub, with connections to Zealand and Germany. Both projects depend on new HVDC converter technology and on the availability of specialised installation vessels, whose booking schedules have become a bottleneck across Europe."}
